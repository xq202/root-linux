#!/bin/sh
set -e
# Automatically added by dh_installinit/12.10ubuntu1
if [ -x "/etc/init.d/zerotier-one" ] && [ "$1" = remove ]; then
	invoke-rc.d zerotier-one stop || exit 1
fi
# End automatically added section
